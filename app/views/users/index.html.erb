<div id="users">
	<% @users.each do |user| %>
		<p>
			<%= link_to user.name, user %> [<%= link_to 'edit', edit_user_path(user) %>]
		</p>
	<% end %>
</div>

<p>
	<%= link_to "Add User", new_user_path %>
</p>